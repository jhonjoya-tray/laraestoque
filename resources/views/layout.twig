<html>
<head>
	<title>{{titulo}}</title>
	<link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
</head>
<body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header col-xs-12">
              <a class="navbar-brand" href="#">
                LaraEstoque
                </a>

            <ul id="menu">
                {% if isAdmin %}
                    <li><a href="{{ urlUsuarioInserir }}">Cadastrar Usuário</a></li>
                    <li><a href="#">Listar Usuários</a></li>
                {% endif %}
                <li><a href="{{ action('ProdutosController@getInserir') }}">Cadastrar Produto</a></li>
                <li><a href="{{ action('ProdutosController@getIndex') }}">Listar Produtos</a></li>
                <li><a href="{{ action('EstoqueController@getIndex') }}">Controle de Estoque</a></li>
                {% if user  %}
                    <li><a href="{{ url('logout') }}">Sair</a></li>
                {% endif %}
                {% if not user  %}
                    <li><a href="{{ url('login') }}">Login</a></li>
                    <li><a href="{{ url('register') }}">Cadastrar-se</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<div class="container">

    <div class="pull-right">
        {% if user  %}
            <div><strong>Usuário Logado: </strong> {{ user.nome }} ({{ (user.email) }}) </div>
        {% endif %}
    </div>

    {% block content %}
    {% endblock %}
</div>

<style type="text/css">
    #menu{
        float:right;
    }
    #menu li{
        float:left;
        padding:5px;
        margin-top:10px;
        border:1px solid white;
        margin-left: 5px;
        background:#55c7fc;
        color:black;
    }

    #menu a {
        cursor: pointer;
        text-decoration: none;
    }

    #menu li:hover{

    }

</style>

<body>